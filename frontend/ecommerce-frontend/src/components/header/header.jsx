import AppNav  from "../navbar/navbar"

// contains logo, search bar, navigation links(help/support/wishlist), cart, profile, notification
const Header = () => {
    return (<>
        <AppNav></AppNav>
    </>)
}

export default Header